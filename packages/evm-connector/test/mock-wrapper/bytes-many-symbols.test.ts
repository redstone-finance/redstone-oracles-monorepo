import { expect } from "chai";

// TODO: the same tests as in bytes.test.ts, but for many dataFeedIds
// TODO: also test if requested dataFeedId is not found

describe("SampleRedstoneConsumerBytesMockManySymbols", function () {
  it("Hehe", async () => {
    expect(2 + 2).to.eq(4);
  });
});
