import { expect } from "chai";

// TODO: the same tests as in bytes.test.ts, but for many symbols
// TODO: also test if requested symbol is not found

describe("SampleRedstoneConsumerBytesMockManySymbols", function () {
  it("Hehe", async () => {
    expect(2 + 2).to.eq(4);
  });
});
