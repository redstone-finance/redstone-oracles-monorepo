{
  "name": "@redstone-finance/sdk",
  "prettier": "redstone-prettier-config",
  "version": "0.3.0",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "NODE_ENV=test jest",
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc --build",
    "preversion": "yarn build",
    "pack-package": "yarn build && cd dist && npm pack",
    "lint": "eslint ."
  },
  "dependencies": {
    "@redstone-finance/oracles-smartweave-contracts": "^0.3.0",
    "@redstone-finance/protocol": "^0.3.0",
    "@redstone-finance/utils": "^0.3.0",
    "axios": "^1.5.0",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "jest": "^29.6.4",
    "msw": "^1.2.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
