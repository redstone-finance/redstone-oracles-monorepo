{
  "name": "@redstone-finance/fuel-connector",
  "version": "0.9.0",
  "description": "A tool to inject RedStone data into Fuel compatible smart contracts",
  "main": "dist/src/index.js",
  "exports": "./dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "yarn global:ts-clean",
    "build": "yarn global:tsc -p tsconfig.build.json",
    "build:turbo": "yarn global:turbo build",
    "typecheck": "yarn global:tsc --noEmit",
    "typecheck:turbo": "yarn global:turbo typecheck",
    "lint": "yarn global:eslint .",
    "lint:fix": "yarn global:eslint --fix .",
    "test-contracts": "yarn global:jest:cov",
    "preversion": "yarn build",
    "pack-package": "yarn build && cd dist && npm pack",
    "contract-typegen": "make -C sway contract && fuels typegen -i \"sway/contract_prices/out/release/prices-abi.json\" -o \"src/autogenerated\" && cp sway/contract_prices/out/release/prices.bin \"src/autogenerated\" && yarn lint:fix",
    "sample-run": "yarn global:ts-node test/sample-run.ts",
    "sample-deploy": "yarn global:ts-node test/sample-deploy.ts",
    "run-node": "fuel-core run --db-type in-memory --debug"
  },
  "dependencies": {
    "@redstone-finance/sdk": "workspace:*",
    "fuels": "^0.97.0"
  },
  "devDependencies": {
    "@redstone-finance/utils": "workspace:*",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "redstone-api": "^0.4.13",
    "toml": "^3.0.0",
    "zod": "^3.22.4"
  },
  "files": [
    "src",
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  }
}
