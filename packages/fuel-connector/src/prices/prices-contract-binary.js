import bin from "@redstone-finance/fuel-connector/src/autogenerated/prices.bin";
import arrayBufferToBuffer from "arraybuffer-to-buffer";

export const pricesContractBinary = fetch(bin).then(async (r) =>
  arrayBufferToBuffer(await r.arrayBuffer())
);
